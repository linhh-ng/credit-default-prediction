# credit-default-prediction
Predicting credit default on 100k Lending Club loans. This project benchmarks a linear model (Logistic Regression) against non-linear algorithms (Random Forest, XGBoost) to see which captures risk better, using SHAP values to explain the results.
